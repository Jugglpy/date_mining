<%= form_for(place) do |form| %>
  <% if place.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(place.errors.count, "error") %> prohibited this name from being saved:</h2>

      <ul>
      <% place.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :place_name %>
  </div>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_area :latitude, id: :place_latitude %>
  </div>

  <div class="field">
    <%= form.label :longtitude %>
    <%= form.text_area :longtitude, id: :place_longitude %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
